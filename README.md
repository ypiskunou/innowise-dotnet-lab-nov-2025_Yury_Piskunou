# InnoShop - Full-Stack E-Commerce Platform

Учебный проект в рамках стажировки .NET. Система представляет собой два микросервиса (**UserService** и **ProductService**) для управления пользователями и каталогом товаров, а также **клиентское приложение на React** для взаимодействия с API.

## 🚀 Функционал

### 👤 UserService (Бэкенд: Управление пользователями)
*   **Auth:** Регистрация, Аутентификация (JWT Access Token).
*   **Security:** Хеширование паролей, защита ролями (Admin/User), защита от самоудаления админа.
*   **Email:** Подтверждение аккаунта через почту (интеграция с MailHog).
*   **Admin:** Блокировка (Soft Delete) и разблокировка пользователей.
*   **API:** Пагинация, фильтрация, поиск пользователей.

### 📦 ProductService (Бэкенд: Каталог товаров)
*   **CRUD:** Полный цикл управления товарами и категориями.
*   **Smart Search:** Глобальные фильтры (скрытие удаленных/неактивных товаров).
*   **My Cabinet:** Личный кабинет продавца для управления своими товарами (включая черновики).
*   **Admin Features:**
    *   Автоматическое скрытие товаров при бане пользователя.
    *   Восстановление товаров при разбане.
*   **API:** Продвинутая пагинация, фильтрация по цене/категории, поиск по названию.

### 🖥️ Frontend (Клиентское приложение)
*   **UX/UI:** Современный интерфейс с сайдбаром категорий (Glassmorphism), "живым" поиском (Typeahead) и пагинацией.
*   **State Management:** Глобальное управление состоянием через React Context + Auto Logout при истечении сессии.
*   **My Products:** Страница управления личными товарами (Редактирование, Удаление, Публикация).
*   **Admin Panel:** Интерфейс для управления пользователями с фильтрацией по статусу.

## 🛠 Технологии

| Стек | Технология |
| :--- | :--- |
| **Backend** | .NET 7 (ASP.NET Core), MediatR (CQRS), Entity Framework, AutoMapper, FluentValidation, NLog |
| **Frontend** | React (Hooks, Context API), React Router, Axios, Bootstrap 5 |
| **База данных** | PostgreSQL |
| **Контейнеризация**| Docker & Docker Compose |
| **Тестирование**| xUnit, Moq, FluentAssertions |

## 🐳 Запуск проекта (Docker)

Проект полностью настроен для запуска в контейнерах.

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/ypiskunou/innowise-dotnet-lab-nov-2025_Yury_Piskunou.git
    cd Inno_Shop
    ```

2.  Запустите все сервисы:
    ```bash
    docker-compose up --build
    ```
    *Миграции и начальные данные применятся автоматически.*

---

## 💻 Локальный запуск (Без Docker)

*Используется для активной разработки (порты Rider).*

1.  **База данных:** Убедитесь, что PostgreSQL запущен локально.
2.  **Backend:** Запустите проекты через Rider / Visual Studio.
    *   **User Service:** `http://localhost:5214`
    *   **Product Service:** `http://localhost:5237`
3.  **Frontend:**
    *   Перейдите в папку `client`.
    *   Выполните `npm install`.
    *   Проверьте `src/api/config.js` (установите свои порты).
    *   Запустите: `npm start`.

## 🔗 Адреса сервисов

| Сервис | Docker Порт | Локальный (Dev) Порт |
| :--- | :--- | :--- |
| **React Frontend** | `3000` | `3000` |
| **User Service** | `5001` | `5214` (HTTP) |
| **Product Service** | `5002` | `5237` (HTTP) |
| **MailHog** | `8025` | `8025` (Локальный .exe) |

## 🔑 Тестовые данные

*   **Admin:** `admin@example.com` / `admin`
*   **User:** `user@example.com` / `user`
*   **Banned User:** `volchkov@inno.by` / `notnA123`

## 🧪 Сценарии использования

1.  **Регистрация и активация:**
    *   Создайте пользователя. Проверьте MailHog, подтвердите почту по ссылке.
2.  **Личный кабинет продавца (Новое!):**
    *   Войдите под обычным юзером.
    *   Перейдите в **"Мои товары"**. Нажмите "Добавить новый".
    *   Создайте товар, но снимите галочку "Активен".
    *   Товар появится в "Моих товарах" как *Черновик*, но в общем каталоге его не будет.
3.  **Администрирование (Бан):**
    *   Зайдите под Админом. Забаньте пользователя.
    *   Проверьте каталог — товары этого пользователя исчезнут.
    *   Разбаньте — товары вернутся (при этом статус "Черновик" у пользователя сохранится).